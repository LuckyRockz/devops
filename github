*github commands 

1. git clone <repo-url> 
  git clone <repo-url> -b <branch_name> 
2. git commit -m "comments"
3. git branch - print the branch 
4. git status - tell you versionsed vs unversioned files. 
5. git pull - pull the latest code.
6. git push - push the code into github for the current brach 
7. git push origin <branch_name> - it will push to a different branch
8. git log <filename> 
9. git rm <filename>


"config commands"

git config --global user.name "LakshamSai"
git config --global user.email "lakshmansai741@gmail.com"

**git ignore. 

add the files to .git folder 

